{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col"></div>
            <div class="d-flex mt-5 col-8">
                <div class="container border rounded">
                    <h3 class="text-center">Tesztike</h3>
                        <div class="container border rounded">
                            <p>Ez itt a lorem ipsum</p>
                        </div>
                    <div class="d-flex mt-3 container border rounded">
                        {{ form_start(form, {'action': path('homepage'), 'method': 'POST', 'attr': {'target':'_blank'}}) }}
                        {{ form_row(form.email) }}
                    </div>
                    <div class="d-flex mt-3 container border rounded">
                        {{ form_row(form.title) }}
                    </div>
                {{ form_end(form) }}
                </div>
            </div>
            <div class="col"></div>
        </div>
    </div>
{% endblock %}
